@using cnu_cinema_practice.Services
@inject ICinemaService CinemaService
@using cnu_cinema_practice.Models

<div class="min-h-screen pt-12 pb-24 container mx-auto px-6 animate-in fade-in duration-700">
    <!-- ... header ... -->
    <div class="mb-12 space-y-4">
        <div class="flex items-center gap-3">
            <div class="w-8 h-[2px] bg-cyan-500"></div>
            <span class="text-xs font-black uppercase tracking-[0.4em] text-cyan-500">Мережа локацій</span>
        </div>
        <h2 className="text-6xl font-black uppercase italic tracking-tighter font-heading">Наші <span
                className="text-cyan-400">Кінотеатри</span></h2>
    </div>

    <div class="grid lg:grid-cols-5 gap-8 h-[700px]">
        <!-- Locations List -->
        <div class="lg:col-span-2 space-y-4 overflow-y-auto pr-4 scrollbar-hide">
            @foreach (var loc in Locations)
            {
                <div @onclick="() => selectedLocation = loc"
                    class="p-8 rounded-[2rem] border-2 transition-all cursor-pointer group @(selectedLocation?.Id == loc.Id ? "border-cyan-400 bg-cyan-400/10 shadow-2xl shadow-cyan-500/10" : "border-white/5 bg-slate-950/40 hover:border-slate-700")">
                    <div class="flex justify-between items-start mb-4">
                        <h3 class="text-2xl font-black italic font-heading group-hover:text-cyan-400 transition-colors">
                            @loc.Name</h3>
                        <div class="flex items-center gap-1 text-amber-400 font-bold text-xs">
                            <span>★</span>
                            <span>@loc.Rating</span>
                        </div>
                    </div>
                    <div class="space-y-2 text-slate-400 text-sm font-medium">
                        <p class="flex items-center gap-3">
                            <span class="text-cyan-500">📍</span> @loc.Address
                        </p>
                        <p class="flex items-center gap-3">
                            <span class="text-cyan-500">📞</span> @loc.Phone
                        </p>
                        <p class="flex items-center gap-3">
                            <span class="text-cyan-500">🎞️</span> @loc.HallCount залів • IMAX Laser • LUX
                        </p>
                    </div>
                    <button
                        class="mt-8 w-full py-4 border border-white/10 rounded-xl text-[10px] font-black uppercase tracking-widest hover:bg-white/5 transition-all">
                        Розклад сеансів
                    </button>
                </div>
            }
        </div>

        <!-- Map Placeholder/Visual -->
        <div class="lg:col-span-3 glass rounded-[3rem] overflow-hidden relative border border-white/5">
            <!-- Dark Map Mockup -->
            <div class="absolute inset-0 bg-[#0f172a]">
                <img src="https://images.unsplash.com/photo-1524661135-423995f22d0b?q=80&w=1500&auto=format&fit=crop"
                    class="w-full h-full object-cover opacity-20 grayscale" alt="Map Background" />
                <div class="absolute inset-0 bg-gradient-to-t from-[#020617] via-transparent to-transparent"></div>

                <!-- Glowing Markers (simplified positions for demo) -->
                @foreach (var loc in Locations)
                {
                    <!-- Just demo markers, not exact positions -->
                }
                <div class="absolute top-[40%] left-[30%] animate-pulse group cursor-pointer">
                    <div class="w-6 h-6 bg-cyan-500 rounded-full blur-md opacity-50"></div>
                    <div class="absolute inset-0 w-6 h-6 bg-cyan-500 rounded-full border-4 border-white"></div>
                    <div
                        class="absolute top-8 left-1/2 -translate-x-1/2 bg-slate-900 border border-white/10 px-3 py-1 rounded text-[8px] font-black uppercase whitespace-nowrap opacity-0 group-hover:opacity-100 transition-opacity">
                        CineMaster Center</div>
                </div>
            </div>

            @if (selectedLocation != null)
            {
                <div
                    class="absolute bottom-8 left-8 right-8 glass p-6 rounded-2xl flex items-center justify-between animate-in slide-in-from-bottom-4">
                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 bg-cyan-500/20 rounded-xl flex items-center justify-center text-cyan-400">
                            📍
                        </div>
                        <div>
                            <div class="text-[10px] font-black uppercase tracking-widest text-slate-500">Вибрана локація
                            </div>
                            <div class="font-black italic text-lg uppercase">@selectedLocation.Name</div>
                        </div>
                    </div>
                    <button
                        class="bg-cyan-500 text-slate-950 px-6 py-3 rounded-xl font-black text-[10px] uppercase tracking-widest hover:brightness-110 transition-all">Маршрут</button>
                </div>
            }
        </div>
    </div>
</div>

@code {
    private List<CinemaLocation> Locations = new();
    private CinemaLocation? selectedLocation;

    protected override async Task OnInitializedAsync()
    {
        Locations = await CinemaService.GetCinemasAsync();
        selectedLocation = Locations.FirstOrDefault();
    }
}
